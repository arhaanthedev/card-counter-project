# card-counter-project
A simple JavaScript project that simulates a card‑counting system used in blackjack. It tracks card values, updates a running count, and returns betting advice based on the current count.
